# Dockerfile.frontend
# Use a minimal, official Nginx image. It's very small and fast.
FROM nginx:1.25-alpine

# Copy our new, simple redirect configuration into the image,
# overwriting the default Nginx config.
COPY fly/frontend/nginx.conf /etc/nginx/conf.d/default.conf

# This command ensures Nginx starts and stays in the foreground.
CMD ["nginx", "-g", "daemon off;"]
